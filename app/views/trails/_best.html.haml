.border.p-4
  .row
    .col-12.col-md-6
      %h6.text-primary.mt-0.font-weight-bold 좋아요 베스트
      %ul.list-unstyled
        - Trail.unscoped.order("likes_count desc").limit(8).each do |trail|
          %li
            = link_to trails_path(did: trail.candidate.district, anchor: "trail_#{trail.id}"), class: "text-dark" do
              %strong= trail.candidate.name
              = truncate(trail.body, length: 40)
            %small.text-primary
              %i.far.fa-smile
              = trail.likes_count
    .col-12.col-md-6
      %h6.text-danger.mt-0.font-weight-bold 화나요 베스트
      %ul.list-unstyled
        - Trail.unscoped.order("dislikes_count desc").limit(8).each do |trail|
          %li
            = link_to trails_path(did: trail.candidate.district, anchor: "trail_#{trail.id}"), class: "text-dark" do
              %strong= trail.candidate.name
              = truncate(trail.body, length: 40)
            %small.text-danger
              %i.far.fa-angry
              = trail.dislikes_count